const initialState = [
    { name: "Avengers", price: "3k" },
]

export default (state = initialState, action) => {
     switch(action.type) {
         case "displayMovie":
               return state
     }
}